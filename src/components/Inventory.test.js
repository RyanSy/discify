import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('returns discogs user inventory', () => {
  // check response header for conditions to use to prove inventory was pulled from discogs
  
  // const div = document.createElement('div');
  // ReactDOM.render(<App />, div);
});
